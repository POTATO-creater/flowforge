# FlowForge · AI 工作流编辑器

一个**真正可用**的浏览器端 AI 工作流编辑器：拖拽节点、连线编排 Prompt / 工具链路，点击「运行」后**真实调用大模型或外部 API**，逐节点高亮状态并输出日志，支持保存、导入导出与画布截图。

**37 个节点，全部在浏览器里实测跑通。** 不做「能配置、跑不通」的假节点——详见「网络与跨域」。

## 快速开始

```bash
npm install
npm run dev        # 本地开发，访问 http://localhost:5173
npm run build      # 类型检查 + 生产构建
npm run preview    # 预览构建产物，访问 http://localhost:5173
```

## 在线访问

仓库已配置 GitHub Actions 自动部署到 **GitHub Pages**：推送到 `main` 即自动构建发布。

> 首次使用需在仓库 **Settings → Pages → Build and deployment → Source** 选择 **GitHub Actions**，
> 之后可在 **Actions** 页签查看部署进度，站点地址为 `https://<你的用户名>.github.io/flowforge/`。

由于 Pages 项目站点位于子路径，CI 使用 `vite build --mode pages` 让静态资源带上 `/flowforge/` 前缀；
本地开发与默认构建仍为根路径，无需任何额外配置。

## 配置 API

点击右上角 ⚙ 打开设置：

- **API Base URL**：OpenAI 兼容端点，例如
  - OpenAI：`https://api.openai.com/v1`
  - DeepSeek：`https://api.deepseek.com/v1`
  - 智谱 GLM：`https://open.bigmodel.cn/api/paas/v4`
  - 通义千问：`https://dashscope.aliyuncs.com/compatible-mode/v1`
- **API Key**：仅保存在本机浏览器 `localStorage`，不上传。
- **默认模型**：如 `gpt-4o-mini`。每个「大模型」节点也可单独指定。
- **网络中转**：见下文「网络与跨域」。留空即不中转。

> 直连方案对主流兼容端点可用（均支持 CORS）。若你的私有网关不支持 CORS，参见下文「私有网关 / 隐藏 Key」。

## 双模式：简单点 / 全都要

工具栏左上角可一键切换 **简单点**（小白模式，默认）与 **全都要**（大佬模式），选择会写入 `localStorage`，刷新后保持。

| | 简单点（默认） | 全都要 |
|---|---|---|
| 节点属性 | 只留 1 个最关键的输入框 | 平铺全部字段 |
| 其余设置 | 折叠进「还有 N 项可选设置」 | 直接展开，带分隔线 |
| 节点库说明 | 一句话人话描述 | 稍详细一些的说明 |

两种模式**共享同一份配置数据**，切换不会丢失任何已填内容。新手可以先用「简单点」把链路跑通，再切到「全都要」精调。

> 两种模式下都**不出现任何技术术语**：没有 `System Prompt`、`temperature`、`token`、`JSON` 这类词。滑块两端写的是「老老实实的 ↔ 天马行空的」。

## 变量引用：不用记任何语法

**节点之间连上一条线，下游输入框就会自动填入上游的结果引用**，多数情况下你什么都不用写。

需要手动引用时，点输入框下方的「把前面节点的结果放进来」，按钮上显示的是**中文节点名**（如 `用户的问题 · 内容`），点一下即插入到光标处。

引用写成 `{{节点名.结果名}}`，例如：

```
请把下面内容总结成三句话：

{{用户的问题.内容}}
```

- 节点名就是画布上显示的标题，**改名时所有引用会自动跟着更新**
- 结果名是该节点的产出类型，如开始节点是「内容」、AI 节点是「结果」、网页节点是「正文」
- 同名节点会自动编号（`让 AI 干活`、`让 AI 干活2`），避免引用歧义
- 旧文件里的 `{{llm_ab12.content}}` 写法**仍然兼容**，导入老工作流不会失效

## 节点类型（37 类，8 个分组）

节点库按功能分组，**每组可以点标题折叠**；顶部有搜索框，输入「图」「密码」「翻译」这类词即可跨分组找节点。

| 分组 | 节点 | 作用 | 引用写法 |
|---|---|---|---|
| 从哪开始 | 开始 | 工作流入口 / 用户输入 | `{{节点名.内容}}` |
| 让 AI 干活 | 让 AI 干活 | 调用模型生成文本 | `{{节点名.结果}}` |
| 让 AI 干活 | 想清楚再答 | 分步推理：产出推理过程与最终答案 | `{{节点名.答案}}` |
| 让 AI 干活 | 带工具问答 | 让模型自主决定调用哪个工具（最多 6 次） | `{{节点名.回答}}` |
| 去网上取东西 | 访问网址 | 真实发起 GET/POST/PUT/DELETE 请求 | `{{节点名.返回}}` |
| 去网上取东西 | 读网页 | 抓网页并转为纯文字 | `{{节点名.正文}}` |
| 去网上取东西 | 看技术热榜 | 从 Hacker News 拿热门榜 / 最新榜 | `{{节点名.榜单}}` |
| 去网上取东西 | 读订阅 | 解析 RSS / Atom 订阅源 | `{{节点名.文章}}` |
| 去网上取东西 | 出图表 | 生成柱状图/折线图/饼图/圆环图图片地址 | `{{节点名.图片}}` |
| 去网上取东西 | 查冷知识 | 随机取冷知识或猫知识 | `{{节点名.内容}}` |
| 数据整理 | 挑字段 | 只保留指定的几个字段 | `{{节点名.结果}}` |
| 数据整理 | 分拣 | 按关键词筛选，支持「含有 / 不含 / 开头是」 | `{{节点名.结果}}` |
| 数据整理 | 排序 | 按长短或数字大小排序 | `{{节点名.结果}}` |
| 数据整理 | 截断 | 取前几条或后几条 | `{{节点名.结果}}` |
| 数据整理 | 去掉重复 | 相同内容只留一条 | `{{节点名.结果}}` |
| 数据整理 | 切成多条 | 按分隔符把一条拆成多条 | `{{节点名.结果}}` |
| 数据整理 | 聚成一段 | 把多条合并成一段 | `{{节点名.结果}}` |
| 数据整理 | 算一算 | 数一数 / 求和 / 平均 / 最大 / 最小 / 去重计数 / 连成串 | `{{节点名.结果}}` |
| 数据整理 | 改字段名 | 批量重命名字段 | `{{节点名.结果}}` |
| 文字处理 | 转换排版 | Markdown ↔ HTML 双向转换 | `{{节点名.结果}}` |
| 文字处理 | 处理网页内容 | 去标签留文字 / 按选择器提取 | `{{节点名.结果}}` |
| 文字处理 | 尖括号数据 | XML ↔ 可读对象双向转换 | `{{节点名.结果}}` |
| 文字处理 | 查找替换 | 支持正则、大小写、全部替换 | `{{节点名.结果}}` |
| 文字处理 | 截取 | 按分隔符取第 N 段，或按字符位置截取 | `{{节点名.结果}}` |
| 日期与编码 | 日期时间 | 取现在时间 / 格式化 / 加减天数 / 算相差 / 星期几 | `{{节点名.结果}}` |
| 日期与编码 | 算指纹 | SHA-1/256/384/512 哈希、HMAC 签名、生成随机字符串 | `{{节点名.结果}}` |
| 日期与编码 | 编码转换 | Base64 / URL / HTML 实体 编解码 | `{{节点名.结果}}` |
| 日期与编码 | 动态口令 | 按 TOTP 生成 6 位动态口令 | `{{节点名.口令}}` |
| 日期与编码 | 解析令牌 | 解析或签名 JWT | `{{节点名.结果}}` |
| 控制流程 | 如果……就…… | 条件分流（成立走右、不成立走下） | `{{节点名.判断}}` |
| 控制流程 | 分多条路 | 按关键词分流到多条路径 + 「其它」兜底 | `{{节点名.判断}}` |
| 控制流程 | 等一会儿 | 停 N 秒再继续 | `{{节点名.内容}}` |
| 控制流程 | 出错就停 | 主动中断并提示一句话 | `{{节点名.内容}}` |
| 控制流程 | 汇总 | 把多个上游结果聚成一份文本（按连线顺序，结果确定） | `{{节点名.汇总}}` |
| 控制流程 | 分段处理 | 把长文切段，每段**串行**跑一轮 AI 生成 | `{{节点名.结果}}` |
| 控制流程 | 程序加工 | `return ...` 处理数据，上游在 `input` 变量 | `{{节点名.结果}}` |
| 看结果 | 看结果 | 拼最终展示文本，终止节点 | `{{节点名.内容}}` |

> 上表是**界面上显示的节点名**与结果名。实际使用时不必手写，点「把前面节点的结果放进来」即可自动插入。

**分多条路节点**：在「怎么分路」里一行写一条，格式为 `关键词=分支名`。运行时会按顺序检查内容里是否出现关键词，命中就走对应的那条路；都没命中时按设置走「其它」或干脆不走。分支名会显示在节点右侧的小圆点上，连线时直接选。

**分段处理节点**：在「每一小段要做什么」里用 `{{item}}` 代表当前这一段，它是节点内部保留名，与节点引用无关。分段是**节点内部循环**，不会在图上产生回边，不破坏拓扑排序。硬上限 50 段。

## 网络与跨域（重要）

FlowForge 是**纯浏览器应用，没有后端**。浏览器出于安全规则，默认不允许网页读取别的网站的内容，这是**节点能不能真正跑通的唯一决定性因素**。

### 两种处理方式

- **只做能直连的**：像 Hacker News（走 Algolia 接口）、冷知识这类接口，对方明确允许网页直接访问，节点就直连，不需要任何设置。
- **自动降级兜底**：另一部分节点（访问网址、读网页、读订阅）先尝试直连，被拦下来时**自动改用「网络中转」地址绕过去**，并在运行记录里写明「直连被浏览器拦了，已自动改用中转」。

中转地址在右上角 ⚙ 设置的**「网络中转」**栏里填，默认是 `https://r.jina.ai/`，留空就是完全不中转。

### 直连被拦时会看到什么

如果直连失败又没配中转，报错写的是人话而不是 `Failed to fetch`：

> 这个网站不允许网页直接读它的内容（浏览器的安全规则）。可以点右上角齿轮打开设置，在「网络中转」里填一个中转地址再试。

节点库里带 **「需要中转」** 小标记的，就是可能需要中转才能访问的节点。

### 为什么不实现 n8n 那 300 多个 App 节点

n8n 的节点库包含 Core 67 个、App 约 275 个、Trigger 89 个。其中绝大多数（Gmail、Slack、Google Sheets、AWS、各类数据库……）**在纯浏览器环境里架构上不可能实现**：

- 它们依赖 **OAuth 授权**，而 OAuth 的回调必须经过服务端，浏览器里做不到；
- 或者目标站点不返回跨域许可，中转也拿不到需要鉴权的数据。

**硬摆上去只会得到「能配置、跑不通」的假节点**，反而误导使用者。因此本项目只收录**纯浏览器环境下真实可跑通**的节点，并把做不到的原因写在这里，而不是假装支持。


## 模板与技能

### 现成的例子（预设工作流）

工具栏点「现成的」打开画廊，内置 5 套开箱即用的工作流：

| 模板 | 内容 |
|---|---|
| 文章摘要器 | 输入网址 → 读网页 → AI 总结 → 看结果 |
| 翻译 + 润色 | 输入原文 → 翻译 → 润色 → 看结果 |
| 双源调研对比 | 两路读网页 → 汇总 → 对比分析 → 看结果 |
| 批量分段摘要 | 输入长文 → 分段处理 → 汇总 → 看结果 |
| 带判断的问答助手 | 输入问题 → 分步作答 → 条件分流 → 看结果 |

载入模板会**覆盖当前画布**，因此会先弹二次确认。也可以点工具栏「打开」选择外部 `.json` 模板：

```json
{
  "name": "我的模板",
  "nodes": [
    { "id": "start_1", "kind": "start", "position": { "x": 0, "y": 120 }, "config": { "text": "输入内容" } },
    { "id": "llm_1", "kind": "llm", "position": { "x": 320, "y": 120 }, "config": { "model": "gpt-4o-mini", "content": "请总结：{{start_1.text}}" } }
  ],
  "edges": [
    { "id": "e1", "source": "start_1", "target": "llm_1" }
  ]
}
```

导入时逐节点校验 `kind`，缺失字段自动补默认值，因此手写模板只要 `nodes` 数组就能通过。

### 技能（提示词包）

**技能 = 可复用的提示词片段**。工具栏点「技能」打开技能库，内置 6 个：代码审查、文案润色、翻译专家、数据分析、结构化输出、教学讲解。

两种新建方式：

- **从文件导入**：上传 `.md` / `.txt`，取首行 `# 名称` 作为技能名（可多个文件批量选）。
- **粘贴新建**：直接填写名称与提示词正文。

**套用到节点**：选中「让 AI 干活 / 想清楚再答 / 带工具问答 / 分段处理」这类节点后，右侧会出现「套用一个技能」，选好技能后点 **接在后面** 或 **直接替换**。技能只是把文本写进节点配置，之后可自由编辑，不是运行时依赖。

技能保存在 `flowforge.skills`，自建技能可随时删除，内置技能不可删。

## 运行模型

1. 从左侧把节点拖到画布上（双击也行），然后连线——从节点右边的小圆点拖到下一个节点左边的小圆点。连上之后，下游输入框会**自动填入上游结果**。
2. 在右侧填好每个节点；「如果……就……」节点有两个出点，成立走右边、不成立走下面。
3. 点「跑一遍」：从「开始」节点按拓扑顺序往下走，条件节点按结果选择分支，逐节点回写状态。
4. 底部「运行记录」显示每个节点做了什么；出错即停并保留上游结果；可随时点「停下」中断。

## 持久化

- 编辑内容**自动保存到 localStorage**，刷新不丢失。
- 「存下来 / 打开」：工作流存为 `.json` 文件，也可以再打开。
- 「截图」：把当前画布导出为 PNG。
- 相关 key：`flowforge.settings`（API 配置）/ `flowforge.workflow`（画布）/ `flowforge.mode`（模式）/ `flowforge.skills`（自定义技能）/ `flowforge.collapsedGroups`（节点库折叠状态）。

## 私有网关 / 隐藏 Key（可选）

默认从浏览器直连模型端点（Key 存本地）。若要隐藏 Key 或对接不支持 CORS 的网关，可在 `vite.config.ts` 启用 `server.proxy`，把 `/llm` 转发到你的网关，并在 `src/engine/llm.ts` 把请求地址改为 `/llm/chat/completions` 即可。

## 技术栈

Vite + React 18 + TypeScript + React Flow（`@xyflow/react`）+ Zustand，纯前端静态可部署，无额外运行时依赖。

**扩展新节点**：在 `src/types.ts` 加 `NodeKind` → `src/nodeMeta.ts` 加元信息（含 `plain` 人话说明与 `group` 分组）→ `src/fieldDefs.ts` 声明该节点的字段表 → `src/engine/execute.ts` 加执行分支 → `src/nodes/FlowNode.tsx` 加卡片渲染分支。Inspector 由字段表**驱动渲染**，新增节点无需改动 Inspector 代码。

**漏加分支会编译报错，不会静默失效**：`NODE_METAS`、`NODE_FIELDS`、`VAR_FIELD`、`RAW_VAR_FIELD`、`MINIMAP_COLOR` 都是 `Record<NodeKind, ...>`，漏登记即编译失败；`executeNode()` 与 `FlowNode()` 底部各有 `assertNever(...)` 兜底，漏写 case 同样编译报错。这道防线是踩过坑之后加的——原先漏写 case 只会让节点**运行时不执行、卡片渲染成空白**，且不报任何错。

**`VAR_FIELD` 与 `RAW_VAR_FIELD` 必须成对**：前者是给用户看的中文结果名（如 `chart: '图片'`），后者必须是执行器返回对象上的**真实键名**（如 `chart: 'url'`）。两者不匹配会让 `{{节点名.结果名}}` 静默解析成 `undefined`，且没有任何报错。

## 常见问题

**页面布局错乱、按钮点不动？**
确认 `src/App.tsx` 中三个面板分别被 `.app__toolbar` / `.app__sidebar` / `.app__inspector` 包裹——
`.app` 的三栏网格依赖这些类名承载 `grid-area`，缺失会导致整个布局塌陷。修改布局时请一并检查。

**能不能自己加节点？**
能。`src/fieldDefs.ts` 是字段驱动渲染的唯一来源：给节点声明好字段表，Inspector 会自动渲染，不需要改界面代码。

**字体与系统不一致？**
界面字体通过 Google Fonts 异步加载（非阻塞）。离线或内网环境下会回退到
`PingFang SC` / `Microsoft YaHei` / `system-ui`，不影响功能。

## 已知限制

- 每个节点在单次运行中最多执行一次（有环会被拦截），适合 DAG 编排；循环节点用**内部串行循环**处理批量，而非图上回边。
- 大模型调用为非流式，便于日志与调试。
- 网页抓取受浏览器 CORS 限制：能直连的直连，不能的自动降级走「网络中转」；中转也拿不到时会报错并停止后续。
- 工具调用 Agent 最多 6 轮，工具端点需可通过参数 `url`、工具定义 `x-endpoint` 或路径占位符解析出 URL。
- 代码 / 条件节点使用 `new Function` 执行本地工作流表达式，仅在你自己搭建的工作流中安全。
- 变量引用基于**节点名**，因此节点改名时程序会自动同步更新所有引用；若手动把名字改成与另一个节点重名，可能出现引用歧义（新建节点时已自动编号规避）。
- 需要账号鉴权的第三方服务（Gmail、Slack、各类数据库等）**未实现**，原因见「网络与跨域」一节。
